import { Tabs, Tab } from 'nextra/components'
import { Callout } from 'nextra/components'
import { 
  GitBranch, 
  GitPullRequest, 
  Code, 
  Heart,
  Users,
  FileText,
  Bug,
  Lightbulb,
  CheckCircle,
  AlertCircle,
  Terminal,
  Book,
  MessageCircle,
  Star
} from "lucide-react";

# 🤝 Contributing

<div className="liquid-glass rounded-3xl p-6 my-8 border border-blue-200/30">
  <div className="flex items-center gap-3 mb-4">
    <div className="w-12 h-12 ios-logo apple-gradient flex items-center justify-center">
      <Heart className="w-6 h-6 text-white" />
    </div>
    <div>
          <h2 className="text-2xl font-bold text-foreground hero-text">Contributing to LiqUIdify</h2>
    <p className="text-foreground/70 body-text">Join our community and help make LiqUIdify better</p>
    </div>
  </div>
</div>

<Callout type="info" emoji="💙">
  Thank you for considering contributing to LiqUIdify! Every contribution, no matter how small, helps make the library better for everyone.
</Callout>

## 🌟 Ways to Contribute

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-8">
  <div className="liquid-glass rounded-2xl p-6 hover:scale-105 transition-all duration-300">
    <div className="flex items-center gap-3 mb-3">
      <div className="w-10 h-10 ios-logo apple-gradient-2 flex items-center justify-center">
        <Code className="w-5 h-5 text-white" />
      </div>
      <h3 className="text-lg font-semibold text-foreground">Code Contributions</h3>
    </div>
    <p className="text-sm text-foreground/70">Submit bug fixes, new features, or improvements</p>
  </div>
  
  <div className="liquid-glass rounded-2xl p-6 hover:scale-105 transition-all duration-300">
    <div className="flex items-center gap-3 mb-3">
      <div className="w-10 h-10 ios-logo apple-gradient-3 flex items-center justify-center">
        <FileText className="w-5 h-5 text-white" />
      </div>
      <h3 className="text-lg font-semibold text-foreground">Documentation</h3>
    </div>
    <p className="text-sm text-foreground/70">Improve guides, fix typos, or add examples</p>
  </div>
  
  <div className="liquid-glass rounded-2xl p-6 hover:scale-105 transition-all duration-300">
    <div className="flex items-center gap-3 mb-3">
      <div className="w-10 h-10 ios-logo apple-gradient-4 flex items-center justify-center">
        <Bug className="w-5 h-5 text-white" />
      </div>
      <h3 className="text-lg font-semibold text-foreground">Bug Reports</h3>
    </div>
    <p className="text-sm text-foreground/70">Help us identify and fix issues</p>
  </div>
  
  <div className="liquid-glass rounded-2xl p-6 hover:scale-105 transition-all duration-300">
    <div className="flex items-center gap-3 mb-3">
      <div className="w-10 h-10 ios-logo apple-gradient flex items-center justify-center">
        <Lightbulb className="w-5 h-5 text-white" />
      </div>
      <h3 className="text-lg font-semibold text-foreground">Feature Requests</h3>
    </div>
    <p className="text-sm text-foreground/70">Suggest new components or improvements</p>
  </div>
</div>

## 🚀 Getting Started

### 1. Fork & Clone

```bash
# Fork the repository on GitHub, then:
git clone https://github.com/YOUR_USERNAME/liquidui.git
cd liquidui
```

### 2. Install Dependencies

<Tabs items={['npm', 'yarn', 'pnpm']}>
  <Tab>
    ```bash
    npm install
    ```
  </Tab>
  <Tab>
    ```bash
    yarn install
    ```
  </Tab>
  <Tab>
    ```bash
    pnpm install
    ```
  </Tab>
</Tabs>

### 3. Create a Branch

```bash
git checkout -b feature/my-awesome-feature
# or
git checkout -b fix/bug-description
```

### 4. Make Your Changes

```bash
# Run development server
npm run dev

# Run tests
npm run test

# Build the library
npm run build
```

## 📋 Contribution Guidelines

### Code Style

<div className="liquid-glass rounded-2xl p-6 my-6">
  <h4 className="text-lg font-semibold mb-4 hero-text flex items-center gap-2">
    <CheckCircle className="w-5 h-5 text-green-500" />
    Best Practices
  </h4>
  
  - **TypeScript**: All code must be written in TypeScript
  - **Formatting**: Use Prettier (runs automatically on commit)
  - **Linting**: Follow ESLint rules (run `npm run lint`)
  - **Components**: Follow existing component patterns
  - **Tests**: Write tests for new features
  - **Documentation**: Update docs for API changes
</div>

### Commit Messages

Follow the [Conventional Commits](https://www.conventionalcommits.org/) specification:

```bash
# Format
<type>(<scope>): <subject>

# Examples
feat(button): add loading state
fix(modal): prevent body scroll when open
docs(readme): update installation instructions
style(card): improve hover effects
test(input): add validation tests
```

**Types:**
- `feat`: New feature
- `fix`: Bug fix
- `docs`: Documentation changes
- `style`: Code style changes (formatting, etc.)
- `refactor`: Code refactoring
- `test`: Test additions or changes
- `chore`: Build process or auxiliary tool changes

### Pull Request Process

<div className="space-y-4 my-6">
  <div className="liquid-glass rounded-2xl p-4">
    <div className="flex items-start gap-3">
      <div className="w-8 h-8 ios-logo apple-gradient flex items-center justify-center flex-shrink-0 mt-1">
        <span className="text-white font-bold text-sm">1</span>
      </div>
      <div>
        <h5 className="font-semibold text-foreground mb-1">Create PR</h5>
        <p className="text-sm text-foreground/70">Use a clear, descriptive title</p>
      </div>
    </div>
  </div>
  
  <div className="liquid-glass rounded-2xl p-4">
    <div className="flex items-start gap-3">
      <div className="w-8 h-8 ios-logo apple-gradient-2 flex items-center justify-center flex-shrink-0 mt-1">
        <span className="text-white font-bold text-sm">2</span>
      </div>
      <div>
        <h5 className="font-semibold text-foreground mb-1">Description</h5>
        <p className="text-sm text-foreground/70">Explain what changes you made and why</p>
      </div>
    </div>
  </div>
  
  <div className="liquid-glass rounded-2xl p-4">
    <div className="flex items-start gap-3">
      <div className="w-8 h-8 ios-logo apple-gradient-3 flex items-center justify-center flex-shrink-0 mt-1">
        <span className="text-white font-bold text-sm">3</span>
      </div>
      <div>
        <h5 className="font-semibold text-foreground mb-1">Tests</h5>
        <p className="text-sm text-foreground/70">Ensure all tests pass and add new ones if needed</p>
      </div>
    </div>
  </div>
  
  <div className="liquid-glass rounded-2xl p-4">
    <div className="flex items-start gap-3">
      <div className="w-8 h-8 ios-logo apple-gradient-4 flex items-center justify-center flex-shrink-0 mt-1">
        <span className="text-white font-bold text-sm">4</span>
      </div>
      <div>
        <h5 className="font-semibold text-foreground mb-1">Review</h5>
        <p className="text-sm text-foreground/70">Wait for review and address any feedback</p>
      </div>
    </div>
  </div>
</div>

### PR Template

```markdown
## Description
Brief description of changes

## Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Testing
- [ ] Tests pass locally
- [ ] Added new tests
- [ ] Manual testing completed

## Checklist
- [ ] Code follows style guidelines
- [ ] Self-reviewed code
- [ ] Updated documentation
- [ ] No console errors/warnings
```

## 🧪 Testing

### Running Tests

```bash
# Run all tests
npm run test

# Run tests in watch mode
npm run test:watch

# Run tests with coverage
npm run test:coverage
```

### Writing Tests

```typescript
import { render, screen } from '@testing-library/react';
import { GlassButton } from '../GlassButton';

describe('GlassButton', () => {
  it('renders with children', () => {
    render(<GlassButton>Click me</GlassButton>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('handles click events', () => {
    const handleClick = jest.fn();
    render(<GlassButton onClick={handleClick}>Click</GlassButton>);
    screen.getByText('Click').click();
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
});
```

## 📚 Documentation

### Component Documentation

When adding or modifying components, update the documentation:

1. **Component page**: `/pages/components/[component-name].mdx`
2. **API reference**: Include all props with descriptions
3. **Examples**: Provide practical usage examples
4. **Accessibility**: Note any a11y considerations

### Example Structure

```mdx
# Component Name

Brief description of the component.

## Basic Usage

<ComponentShowcase>
  <YourComponent />
</ComponentShowcase>

## API Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| prop1 | string | - | Description |

## Examples

### Example 1
[Code and demo]

### Example 2
[Code and demo]

## Accessibility

- Keyboard navigation details
- ARIA attributes used
- Screen reader considerations
```

## 🏗️ Project Structure

```
liquidui/
├── src/
│   ├── components/      # Component source files
│   ├── hooks/          # Custom React hooks
│   ├── utils/          # Utility functions
│   ├── styles/         # Global styles
│   └── types/          # TypeScript types
├── docs/               # Documentation site
├── tests/              # Test files
├── examples/           # Example projects
└── packages/           # Published packages
```

## 🤝 Code of Conduct

<Callout type="warning" emoji="⚠️">
  Please note that this project is released with a [Code of Conduct](https://github.com/tuliocunha23/liquidui/blob/main/CODE_OF_CONDUCT.md). By participating, you agree to abide by its terms.
</Callout>

### Our Standards

- Be respectful and inclusive
- Welcome newcomers and help them get started
- Focus on constructive criticism
- Accept feedback gracefully
- Put the community first

## 🎉 Recognition

All contributors are recognized in our:

- [Contributors page](https://github.com/tuliocunha23/liquidui/graphs/contributors)
- Release notes
- Annual contributor spotlight

<div className="liquid-glass rounded-3xl p-8 mt-12 text-center">
  <h3 className="text-2xl font-bold mb-4 hero-text">Ready to Contribute?</h3>
  <p className="text-foreground/70 mb-6 body-text">
    Join our welcoming community and help shape the future of LiqUIdify!
  </p>
  <div className="flex gap-4 justify-center">
    <a href="https://github.com/tuliocunha23/liquidui" className="glass-button-primary px-6 py-3 rounded-2xl hover:scale-105 transition-all duration-300 inline-flex items-center gap-2">
      <GitBranch className="w-5 h-5" />
      Fork on GitHub
    </a>
    <a href="https://github.com/tuliocunha23/liquidui/issues" className="glass-button px-6 py-3 rounded-2xl hover:scale-105 transition-all duration-300 inline-flex items-center gap-2">
      <MessageCircle className="w-5 h-5" />
      Discuss on GitHub
    </a>
  </div>
</div>

## 🙏 Thank You!

<div className="liquid-glass rounded-2xl p-6 mt-8 text-center">
  <Star className="w-12 h-12 text-yellow-500 mx-auto mb-4" />
  <p className="text-lg text-foreground mb-2">
    Every contribution makes LiqUIdify better.
  </p>
  <p className="text-foreground/70">
    Thank you for being part of our journey! 💙
  </p>
</div>