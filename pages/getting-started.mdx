---
title: Getting Started
---

import { GlassButton, GlassCard, GlassBadge } from "../components/GlassComponents";
import { ComponentShowcase } from "../components/ComponentShowcase";
import { motion } from "framer-motion";
import { Package, Download, Code, Rocket, CheckCircle } from "lucide-react";

# Getting Started

Welcome to LiquidiUI! Get up and running with our physics-based glassmorphism component library in minutes.

<div className="glass-card p-6 my-6">
  <div className="flex items-center gap-3 mb-4">
    <Rocket className="w-6 h-6 text-primary" />
    <h2 className="text-xl font-semibold m-0">Quick Start</h2>
  </div>
  <p className="text-muted-foreground mb-4">
    LiquidiUI is a modern React component library that brings physics-based interactions and stunning glassmorphism design to your applications.
  </p>
  <div className="flex flex-wrap gap-2">
    <GlassBadge variant="outline">
      <Package className="w-3 h-3 mr-1" />
      React 18+
    </GlassBadge>
    <GlassBadge variant="outline">
      <Code className="w-3 h-3 mr-1" />
      TypeScript Ready
    </GlassBadge>
    <GlassBadge variant="outline">
      <CheckCircle className="w-3 h-3 mr-1" />
      Accessible
    </GlassBadge>
  </div>
</div>

## Installation

### Using npm

```bash
npm install @tuliocunha23/liquidui
```

### Using yarn

```bash
yarn add @tuliocunha23/liquidui
```

### Using pnpm

```bash
pnpm add @tuliocunha23/liquidui
```

## Setup

### 1. Import Styles

Add the LiquidiUI CSS to your project. In your main CSS file or `_app.tsx`:

```tsx
import '@tuliocunha23/liquidui/css'
```

### 2. Configure Tailwind CSS

If you're using Tailwind CSS (recommended), add LiquidiUI to your `tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './node_modules/@tuliocunha23/liquidui/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

### 3. Setup Theme Provider (Optional)

For better theme support, wrap your app with the theme provider:

```tsx
import { ThemeProvider } from '@tuliocunha23/liquidui'

function MyApp({ Component, pageProps }) {
  return (
    <ThemeProvider>
      <Component {...pageProps} />
    </ThemeProvider>
  )
}
```

## First Component

Let's create your first LiquidiUI component:

<ComponentShowcase
  title="Your First LiquidiUI Component"
  description="A simple button with glassmorphism effect"
  code={`import { GlassButton } from '@tuliocunha23/liquidui'

function MyComponent() {
  return (
    <GlassButton onClick={() => alert('Hello LiquidiUI!')}>
      Click me!
    </GlassButton>
  )
}`}
>
  <GlassButton onClick={() => alert('Hello LiquidiUI!')}>
    Click me!
  </GlassButton>
</ComponentShowcase>

## Basic Example

Here's a more complete example showing multiple components:

<ComponentShowcase
  title="Complete Example"
  description="Card with button and content"
  code={`import { GlassCard, GlassButton, GlassBadge } from '@tuliocunha23/liquidui'

function WelcomeCard() {
  return (
    <GlassCard className="p-6 max-w-md">
      <div className="space-y-4">
        <GlassBadge>New Feature</GlassBadge>
        <h3 className="text-xl font-semibold">Welcome to LiquidiUI</h3>
        <p className="text-muted-foreground">
          Experience the future of React components with physics-based interactions.
        </p>
        <GlassButton className="w-full">
          Get Started
        </GlassButton>
      </div>
    </GlassCard>
  )
}`}
>
  <GlassCard className="p-6 max-w-md">
    <div className="space-y-4">
      <GlassBadge>New Feature</GlassBadge>
      <h3 className="text-xl font-semibold">Welcome to Glass UI</h3>
      <p className="text-muted-foreground">
        Experience the future of React components with physics-based interactions.
      </p>
      <GlassButton className="w-full">
        Get Started
      </GlassButton>
    </div>
  </GlassCard>
</ComponentShowcase>

## Key Features

### Physics-Based Interactions

LiquidiUI components respond to user interaction with natural, physics-based animations:

- **Magnetic Hover**: Components attract the cursor within a proximity radius
- **Spring Animations**: Smooth, natural transitions using spring physics
- **Gesture Recognition**: Advanced touch and mouse gesture support

### Content-Aware Adaptation

Components automatically adapt to their environment:

- **Background Detection**: Adjusts opacity based on background content
- **Color Contrast**: Maintains readability across different backgrounds
- **Dynamic Sizing**: Responds to content changes with smooth transitions

### Accessibility First

Built with accessibility as a core principle:

- **Screen Reader Support**: Full ARIA compliance
- **Keyboard Navigation**: Complete keyboard interaction support
- **Focus Management**: Intelligent focus handling and indicators
- **High Contrast**: Supports system high contrast modes

## TypeScript Support

LiquidiUI is built with TypeScript and provides full type safety:

```tsx
import { GlassButtonProps } from '@tuliocunha23/liquidui'

interface MyButtonProps extends GlassButtonProps {
  customProp?: string
}

function MyButton({ customProp, ...props }: MyButtonProps) {
  return <GlassButton {...props} />
}
```

## Performance

LiquidiUI is optimized for performance:

- **Tree Shaking**: Only import components you use
- **Lazy Loading**: Components load on demand
- **Minimal Bundle Size**: Efficient packaging and compression
- **Hardware Acceleration**: Uses GPU acceleration for smooth animations

## Browser Support

LiquidiUI supports all modern browsers:

- Chrome 88+
- Firefox 85+
- Safari 14+
- Edge 88+

## Next Steps

<div className="grid gap-4 md:grid-cols-2 mt-8">
  <GlassCard className="p-6">
    <h3 className="text-lg font-semibold mb-2">📚 Explore Components</h3>
    <p className="text-muted-foreground mb-4">
      Browse our comprehensive component library with 30+ components.
    </p>
    <GlassButton variant="outline" size="sm">
      <a href="/components">View Components</a>
    </GlassButton>
  </GlassCard>

  <GlassCard className="p-6">
    <h3 className="text-lg font-semibold mb-2">🎨 Theming Guide</h3>
    <p className="text-muted-foreground mb-4">
      Learn how to customize LiquidiUI to match your brand.
    </p>
    <GlassButton variant="outline" size="sm">
      <a href="/theming">Theming Guide</a>
    </GlassButton>
  </GlassCard>

  <GlassCard className="p-6">
    <h3 className="text-lg font-semibold mb-2">💡 Examples</h3>
    <p className="text-muted-foreground mb-4">
      See LiquidiUI in action with real-world examples.
    </p>
    <GlassButton variant="outline" size="sm">
      <a href="/examples">View Examples</a>
    </GlassButton>
  </GlassCard>

  <GlassCard className="p-6">
    <h3 className="text-lg font-semibold mb-2">🚀 Advanced Usage</h3>
    <p className="text-muted-foreground mb-4">
      Discover advanced patterns and customization options.
    </p>
    <GlassButton variant="outline" size="sm">
      <a href="/advanced">Advanced Guide</a>
    </GlassButton>
  </GlassCard>
</div>

## Community

Join our growing community:

- **GitHub**: [liquidi-ui repository](https://github.com/tuliopc23/liquidi-ui)
- **Discord**: [Join our Discord server](https://discord.gg/liquidi-ui)
- **Twitter**: [@liquidiui](https://twitter.com/liquidiui)

## Support

Need help? We're here for you:

- 📖 [Documentation](/components)
- 🐛 [Report Issues](https://github.com/tuliopc23/liquidi-ui/issues)
- 💬 [Discussions](https://github.com/tuliopc23/liquidi-ui/discussions)
- 📧 [Email Support](mailto:support@liquidi-ui.dev)
