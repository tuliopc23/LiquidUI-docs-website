---
title: Getting Started
---

import { ComponentShowcase } from "../components/ComponentShowcase";
import { motion } from "framer-motion";
import { Package, Download, Code, Rocket, CheckCircle } from "lucide-react";

# Getting Started

Welcome to LiqUIdify! Get up and running with our physics-based glassmorphism component library in minutes.

<div className="glass-card p-6 my-6">
  <div className="flex items-center gap-3 mb-4">
    <Rocket className="w-6 h-6 text-primary" />
    <h2 className="text-xl font-semibold m-0">Quick Start</h2>
  </div>
  <p className="text-muted-foreground mb-4">
    LiqUIdify is a modern React component library that brings physics-based interactions and stunning glassmorphism design to your applications.
  </p>
  <div className="flex flex-wrap gap-2">
    <span className="min-w-[44px] min-h-[44px] glass-button px-3 py-1 rounded-full text-sm text-foreground border border-gray-200/50 hover:scale-105 transition-all duration-200 flex items-center">
      <Package className="w-3 h-3 mr-1" />
      React 18+
    </span>
    <span className="min-w-[44px] min-h-[44px] glass-button px-3 py-1 rounded-full text-sm text-foreground border border-gray-200/50 hover:scale-105 transition-all duration-200 flex items-center">
      <Code className="w-3 h-3 mr-1" />
      TypeScript Ready
    </span>
    <span className="min-w-[44px] min-h-[44px] glass-button px-3 py-1 rounded-full text-sm text-foreground border border-gray-200/50 hover:scale-105 transition-all duration-200 flex items-center">
      <CheckCircle className="w-3 h-3 mr-1" />
      Accessible
    </span>
  </div>
</div>

## Installation

### Using npm

```bash
npm install liquidify
```

### Using yarn

```bash
yarn add liquidify
```

### Using pnpm

```bash
pnpm add liquidify
```

## Setup

### 1. Import Styles

Add the LiqUIdify CSS to your project. In your main CSS file or `_app.tsx`:

```tsx
import 'liquidify/css'
```

### 2. Configure Tailwind CSS

If you're using Tailwind CSS (recommended), add LiqUIdify to your `tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './node_modules/liquidify/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

### 3. Setup Theme Provider (Optional)

For better theme support, wrap your app with the theme provider:

```tsx
import { ThemeProvider } from 'liquidify'

function MyApp({ Component, pageProps }) {
  return (
    <ThemeProvider>
      <Component {...pageProps} />
    </ThemeProvider>
  )
}
```

## First Component

Let's create your first LiqUIdify component:

<ComponentShowcase
  title="Your First LiqUIdify Component"
  description="A simple button with glassmorphism effect"
  code={`import { GlassButton } from 'liquidify'

function MyComponent() {
  return (
    <GlassButton onClick={() => alert('Hello LiquidUI!')}>
      Click me!
    </GlassButton>
  )
}`}
>
  <div className="liquid-glass rounded-2xl p-6 border border-white/20 backdrop-blur-xl">
    <div className="flex items-center justify-center">
      <button className="min-w-[44px] min-h-[44px] glass-button-primary px-6 py-3 rounded-2xl text-white font-medium hover:scale-105 transition-all duration-300" onClick={() => alert('Hello LiquidUI!')}>
        Click me!
      </button>
    </div>
  </div>
</ComponentShowcase>

## Basic Example

Here's a more complete example showing multiple components:

<ComponentShowcase
  title="Complete Example"
  description="Card with button and content"
  code={`import { GlassCard, GlassButton, GlassBadge } from 'liquidify'

function WelcomeCard() {
  return (
    <GlassCard className="p-6 max-w-md">
      <div className="space-y-4">
        <GlassBadge>New Feature</GlassBadge>
        <h3 className="text-xl font-semibold">Welcome to LiquidiUI</h3>
        <p className="text-muted-foreground">
          Experience the future of React components with physics-based interactions.
        </p>
        <GlassButton className="w-full">
          Get Started
        </GlassButton>
      </div>
    </GlassCard>
  )
}`}
>
  <div className="liquid-glass rounded-3xl p-6 max-w-md border border-white/20 backdrop-blur-xl">
    <div className="space-y-4">
      <span className="min-w-[44px] min-h-[44px] glass-button-primary px-3 py-1 rounded-full text-sm text-white">New Feature</span>
              <h3 className="text-xl font-semibold text-gray-900 dark:text-white">Welcome to LiqUIdify</h3>
      <p className="text-gray-600 dark:text-gray-300">
        Experience the future of React components with physics-based interactions.
      </p>
      <button className="min-w-[44px] min-h-[44px] glass-button-primary w-full px-6 py-3 rounded-2xl text-white font-medium hover:scale-105 transition-all duration-300">
        Get Started
      </button>
    </div>
  </div>
</ComponentShowcase>

## Key Features

### Physics-Based Interactions

LiquidiUI components respond to user interaction with natural, physics-based animations:

- **Magnetic Hover**: Components attract the cursor within a proximity radius
- **Spring Animations**: Smooth, natural transitions using spring physics
- **Gesture Recognition**: Advanced touch and mouse gesture support

### Content-Aware Adaptation

Components automatically adapt to their environment:

- **Background Detection**: Adjusts opacity based on background content
- **Color Contrast**: Maintains readability across different backgrounds
- **Dynamic Sizing**: Responds to content changes with smooth transitions

### Accessibility First

Built with accessibility as a core principle:

- **Screen Reader Support**: Full ARIA compliance
- **Keyboard Navigation**: Complete keyboard interaction support
- **Focus Management**: Intelligent focus handling and indicators
- **High Contrast**: Supports system high contrast modes

## TypeScript Support

LiquidiUI is built with TypeScript and provides full type safety:

```tsx
import { GlassButtonProps } from 'liquidify'

interface MyButtonProps extends GlassButtonProps {
  customProp?: string
}

function MyButton({ customProp, ...props }: MyButtonProps) {
  return <GlassButton {...props} />
}
```

## Performance

LiquidiUI is optimized for performance:

- **Tree Shaking**: Only import components you use
- **Lazy Loading**: Components load on demand
- **Minimal Bundle Size**: Efficient packaging and compression
- **Hardware Acceleration**: Uses GPU acceleration for smooth animations

## Browser Support

LiquidiUI supports all modern browsers:

- Chrome 88+
- Firefox 85+
- Safari 14+
- Edge 88+

## Next Steps

<div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 mt-8">
  <div className="liquid-glass rounded-3xl p-6 border border-white/20 backdrop-blur-xl">
    <h3 className="text-lg font-semibold mb-2">📚 Explore Components</h3>
    <p className="text-muted-foreground mb-4">
      Browse our comprehensive component library with 30+ components.
    </p>
    <a href="/components" className="min-w-[44px] min-h-[44px] glass-button px-4 py-2 rounded-2xl text-foreground hover:scale-105 transition-all duration-300 inline-block">
      View Components
    </a>
  </div>

  <div className="liquid-glass rounded-3xl p-6 border border-white/20 backdrop-blur-xl">
    <h3 className="text-lg font-semibold mb-2">🎨 Theming Guide</h3>
    <p className="text-muted-foreground mb-4">
      Learn how to customize LiqUIdify to match your brand.
    </p>
    <a href="/theming" className="min-w-[44px] min-h-[44px] glass-button px-4 py-2 rounded-2xl text-foreground hover:scale-105 transition-all duration-300 inline-block">
      Theming Guide
    </a>
  </div>

  <div className="liquid-glass rounded-3xl p-6 border border-white/20 backdrop-blur-xl">
    <h3 className="text-lg font-semibold mb-2">💡 Examples</h3>
    <p className="text-muted-foreground mb-4">
              See LiqUIdify in action with real-world examples.
    </p>
    <a href="/examples" className="min-w-[44px] min-h-[44px] glass-button px-4 py-2 rounded-2xl text-foreground hover:scale-105 transition-all duration-300 inline-block">
      View Examples
    </a>
  </div>

  <div className="liquid-glass rounded-3xl p-6 border border-white/20 backdrop-blur-xl">
    <h3 className="text-lg font-semibold mb-2">🚀 API Reference</h3>
    <p className="text-muted-foreground mb-4">
      Explore detailed API documentation and advanced configuration options.
    </p>
    <a href="/api" className="min-w-[44px] min-h-[44px] glass-button px-4 py-2 rounded-2xl text-foreground hover:scale-105 transition-all duration-300 inline-block">
      API Reference
    </a>
  </div>
</div>

## Community

Join our growing community:

- **GitHub**: [liquidi-ui repository](https://github.com/tuliocunha23/liquidui)
- **GitHub Issues**: [Report bugs or get help](https://github.com/tuliocunha23/liquidui/issues)
- **Twitter**: [@liquidui](https://twitter.com/liquidui)

## Support

Need help? We're here for you:

- 📖 [Documentation](/components)
- 🐛 [Report Issues](https://github.com/tuliocunha23/liquidui/issues)
- 💬 [Discussions](https://github.com/tuliocunha23/liquidui/discussions)
- 📧 [Email Support](mailto:support@liquidui.dev)
