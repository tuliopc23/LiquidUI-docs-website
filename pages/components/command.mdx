
import { GlassCommand } from "../../components/PlaceholderComponents";
import { ComponentShowcase } from "../../components/ComponentShowcase";

# Command

A command is a component that allows the user to search for and select a command.

<ComponentShowcase>
  <GlassCommand />
</ComponentShowcase>

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `commands` | `any[]` | `[]` | The commands to be displayed. |
