---
title: "Table Component - LiqUIdify"
description: "Beautiful glassmorphism table component for displaying data in a structured, visually appealing format with support for striped rows and flexible column configuration."
---

import { GlassTable } from "../../components/PlaceholderComponents";
import { ComponentShowcase } from "../../components/ComponentShowcase";

# Table

A beautiful glassmorphism table component for displaying data in a structured, visually appealing format with support for striped rows and flexible column configuration.

## Basic Usage

<ComponentShowcase>
  <GlassTable
    data={[
      { name: "John Doe", age: 30, email: "john@example.com", role: "Developer" },
      { name: "Jane Smith", age: 25, email: "jane@example.com", role: "Designer" },
      { name: "Bob Johnson", age: 35, email: "bob@example.com", role: "Manager" },
    ]}
    columns={[
      { key: "name", label: "Name", width: "25%" },
      { key: "age", label: "Age", width: "15%" },
      { key: "email", label: "Email", width: "35%" },
      { key: "role", label: "Role", width: "25%" },
    ]}
  />
</ComponentShowcase>

## TypeScript Interface

```typescript
interface GlassTableProps extends React.TableHTMLAttributes<HTMLTableElement> {
  data?: Array<Record<string, any>>;
  columns?: Array<{ 
    key: string; 
    label: string; 
    width?: string 
  }>;
  striped?: boolean;
}
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `data` | `Array<Record<string, any>>` | `[]` | Array of objects representing table rows |
| `columns` | `Array<{ key: string; label: string; width?: string }>` | `[]` | Column configuration array |
| `striped` | `boolean` | `true` | Whether to show alternating row background colors |
| `...tableProps` | `React.TableHTMLAttributes<HTMLTableElement>` | - | Standard HTML table attributes |

### Column Configuration

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `key` | `string` | ✓ | Property key from the data object to display |
| `label` | `string` | ✓ | Column header text |
| `width` | `string` | - | CSS width value for the column |

## Examples

### Simple Data Table

<ComponentShowcase>
  <GlassTable
    data={[
      { product: "Laptop", price: "$999", stock: 15 },
      { product: "Phone", price: "$699", stock: 23 },
      { product: "Tablet", price: "$399", stock: 8 },
    ]}
    columns={[
      { key: "product", label: "Product" },
      { key: "price", label: "Price" },
      { key: "stock", label: "Stock" },
    ]}
  />
</ComponentShowcase>

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function ProductTable() {
  const productData = [
    { product: "Laptop", price: "$999", stock: 15 },
    { product: "Phone", price: "$699", stock: 23 },
    { product: "Tablet", price: "$399", stock: 8 },
  ];

  const columns = [
    { key: "product", label: "Product" },
    { key: "price", label: "Price" },
    { key: "stock", label: "Stock" },
  ];

  return <GlassTable data={productData} columns={columns} />;
}
```

### Custom Column Widths

<ComponentShowcase>
  <GlassTable
    data={[
      { id: 1, name: "John Doe", description: "Senior Frontend Developer with 5+ years experience", status: "Active" },
      { id: 2, name: "Jane Smith", description: "UX Designer specializing in mobile applications", status: "Active" },
      { id: 3, name: "Bob Wilson", description: "Project Manager coordinating cross-functional teams", status: "Inactive" },
    ]}
    columns={[
      { key: "id", label: "ID", width: "10%" },
      { key: "name", label: "Name", width: "20%" },
      { key: "description", label: "Description", width: "50%" },
      { key: "status", label: "Status", width: "20%" },
    ]}
  />
</ComponentShowcase>

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function EmployeeTable() {
  const employees = [
    { id: 1, name: "John Doe", description: "Senior Frontend Developer", status: "Active" },
    { id: 2, name: "Jane Smith", description: "UX Designer", status: "Active" },
    { id: 3, name: "Bob Wilson", description: "Project Manager", status: "Inactive" },
  ];

  const columns = [
    { key: "id", label: "ID", width: "10%" },
    { key: "name", label: "Name", width: "20%" },
    { key: "description", label: "Description", width: "50%" },
    { key: "status", label: "Status", width: "20%" },
  ];

  return <GlassTable data={employees} columns={columns} />;
}
```

### Without Stripes

<ComponentShowcase>
  <GlassTable
    data={[
      { metric: "Page Views", value: "12,345", change: "+5.2%" },
      { metric: "Users", value: "2,891", change: "+12.1%" },
      { metric: "Sessions", value: "4,567", change: "-2.3%" },
    ]}
    columns={[
      { key: "metric", label: "Metric" },
      { key: "value", label: "Value" },
      { key: "change", label: "Change" },
    ]}
    striped={false}
  />
</ComponentShowcase>

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function MetricsTable() {
  return (
    <GlassTable
      data={[
        { metric: "Page Views", value: "12,345", change: "+5.2%" },
        { metric: "Users", value: "2,891", change: "+12.1%" },
        { metric: "Sessions", value: "4,567", change: "-2.3%" },
      ]}
      columns={[
        { key: "metric", label: "Metric" },
        { key: "value", label: "Value" },
        { key: "change", label: "Change" },
      ]}
      striped={false}
    />
  );
}
```

### Custom Table Structure

<ComponentShowcase>
  <GlassTable>
    <thead>
      <tr>
        <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 1</th>
        <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 2</th>
        <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 3</th>
      </tr>
    </thead>
    <tbody>
      <tr className="hover:bg-white/10 transition-colors duration-200">
        <td className="px-6 py-4 text-gray-700">Custom Cell 1</td>
        <td className="px-6 py-4 text-gray-700">Custom Cell 2</td>
        <td className="px-6 py-4 text-gray-700">Custom Cell 3</td>
      </tr>
      <tr className="bg-white/5 hover:bg-white/10 transition-colors duration-200">
        <td className="px-6 py-4 text-gray-700">Custom Cell 4</td>
        <td className="px-6 py-4 text-gray-700">Custom Cell 5</td>
        <td className="px-6 py-4 text-gray-700">Custom Cell 6</td>
      </tr>
    </tbody>
  </GlassTable>
</ComponentShowcase>

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function CustomTable() {
  return (
    <GlassTable>
      <thead>
        <tr>
          <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 1</th>
          <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 2</th>
          <th className="px-6 py-4 text-left text-gray-900 font-semibold">Custom Header 3</th>
        </tr>
      </thead>
      <tbody>
        <tr className="hover:bg-white/10 transition-colors duration-200">
          <td className="px-6 py-4 text-gray-700">Custom Cell 1</td>
          <td className="px-6 py-4 text-gray-700">Custom Cell 2</td>
          <td className="px-6 py-4 text-gray-700">Custom Cell 3</td>
        </tr>
      </tbody>
    </GlassTable>
  );
}
```

## Advanced Usage

### Dynamic Data Loading

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { useState, useEffect } from 'react';

interface User {
  id: number;
  name: string;
  email: string;
  department: string;
  joinDate: string;
}

function UserTable() {
  const [users, setUsers] = useState<User[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchUsers();
  }, []);

  const fetchUsers = async () => {
    setLoading(true);
    try {
      // Simulate API call
      await new Promise(resolve => setTimeout(resolve, 1000));
      
      const userData: User[] = [
        { id: 1, name: "Alice Johnson", email: "alice@company.com", department: "Engineering", joinDate: "2023-01-15" },
        { id: 2, name: "Bob Smith", email: "bob@company.com", department: "Design", joinDate: "2023-02-20" },
        { id: 3, name: "Carol Brown", email: "carol@company.com", department: "Marketing", joinDate: "2023-03-10" },
      ];
      
      setUsers(userData);
    } catch (error) {
      console.error("Failed to fetch users:", error);
    } finally {
      setLoading(false);
    }
  };

  const columns = [
    { key: "id", label: "ID", width: "10%" },
    { key: "name", label: "Name", width: "25%" },
    { key: "email", label: "Email", width: "30%" },
    { key: "department", label: "Department", width: "20%" },
    { key: "joinDate", label: "Join Date", width: "15%" },
  ];

  if (loading) {
    return (
      <div className="text-center py-8">
        <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-500 mx-auto"></div>
        <p className="mt-2 text-gray-600">Loading users...</p>
      </div>
    );
  }

  return <GlassTable data={users} columns={columns} />;
}
```

### Responsive Table Pattern

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { useMobile } from '@tuliocunha23/liquidui';

function ResponsiveTable() {
  const isMobile = useMobile();
  
  const data = [
    { name: "Product A", price: 99.99, category: "Electronics", stock: 15 },
    { name: "Product B", price: 149.99, category: "Clothing", stock: 8 },
    { name: "Product C", price: 79.99, category: "Books", stock: 23 },
  ];

  const desktopColumns = [
    { key: "name", label: "Product Name", width: "40%" },
    { key: "price", label: "Price", width: "20%" },
    { key: "category", label: "Category", width: "25%" },
    { key: "stock", label: "Stock", width: "15%" },
  ];

  const mobileColumns = [
    { key: "name", label: "Product", width: "60%" },
    { key: "price", label: "Price", width: "40%" },
  ];

  return (
    <div>
      <GlassTable 
        data={data} 
        columns={isMobile ? mobileColumns : desktopColumns} 
      />
      
      {isMobile && (
        <div className="mt-4 text-sm text-gray-600">
          <p>Showing simplified view for mobile. Tap items for details.</p>
        </div>
      )}
    </div>
  );
}
```

### Table with Actions

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { Edit, Trash2, Eye } from 'lucide-react';

interface TableItem {
  id: number;
  name: string;
  status: string;
  actions?: React.ReactNode;
}

function ActionTable() {
  const handleEdit = (id: number) => {
    console.log("Edit item:", id);
  };

  const handleDelete = (id: number) => {
    if (confirm("Are you sure you want to delete this item?")) {
      console.log("Delete item:", id);
    }
  };

  const handleView = (id: number) => {
    console.log("View item:", id);
  };

  const data: TableItem[] = [
    {
      id: 1,
      name: "Item One",
      status: "Active",
      actions: (
        <div className="flex gap-2">
          <button
            onClick={() => handleView(1)}
            className="p-1 hover:bg-blue-100 rounded transition-colors"
            title="View"
          >
            <Eye size={16} className="text-blue-600" />
          </button>
          <button
            onClick={() => handleEdit(1)}
            className="p-1 hover:bg-green-100 rounded transition-colors"
            title="Edit"
          >
            <Edit size={16} className="text-green-600" />
          </button>
          <button
            onClick={() => handleDelete(1)}
            className="p-1 hover:bg-red-100 rounded transition-colors"
            title="Delete"
          >
            <Trash2 size={16} className="text-red-600" />
          </button>
        </div>
      ),
    },
    {
      id: 2,
      name: "Item Two",
      status: "Inactive",
      actions: (
        <div className="flex gap-2">
          <button onClick={() => handleView(2)} className="p-1 hover:bg-blue-100 rounded transition-colors">
            <Eye size={16} className="text-blue-600" />
          </button>
          <button onClick={() => handleEdit(2)} className="p-1 hover:bg-green-100 rounded transition-colors">
            <Edit size={16} className="text-green-600" />
          </button>
          <button onClick={() => handleDelete(2)} className="p-1 hover:bg-red-100 rounded transition-colors">
            <Trash2 size={16} className="text-red-600" />
          </button>
        </div>
      ),
    },
  ];

  const columns = [
    { key: "id", label: "ID", width: "15%" },
    { key: "name", label: "Name", width: "40%" },
    { key: "status", label: "Status", width: "25%" },
    { key: "actions", label: "Actions", width: "20%" },
  ];

  return <GlassTable data={data} columns={columns} />;
}
```

### Empty State Handling

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { Database } from 'lucide-react';

function EmptyStateTable() {
  const emptyData: any[] = [];
  
  const columns = [
    { key: "name", label: "Name" },
    { key: "email", label: "Email" },
    { key: "role", label: "Role" },
  ];

  const EmptyState = () => (
    <div className="text-center py-12">
      <Database className="mx-auto h-12 w-12 text-gray-400 mb-4" />
      <h3 className="text-lg font-medium text-gray-900 mb-2">No data available</h3>
      <p className="text-gray-600 mb-4">There are no records to display at this time.</p>
      <button className="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors">
        Add New Record
      </button>
    </div>
  );

  if (emptyData.length === 0) {
    return (
      <div className="backdrop-blur-md bg-white/10 border border-white/20 rounded-2xl overflow-hidden">
        <EmptyState />
      </div>
    );
  }

  return <GlassTable data={emptyData} columns={columns} />;
}
```

## Styling

The GlassTable component uses glassmorphism design with these default styles:

- **Container**: `backdrop-blur-md bg-white/10 border border-white/20 rounded-2xl`
- **Header**: `backdrop-blur-md bg-white/20` with `font-semibold text-gray-900`
- **Rows**: Alternating background (`bg-white/5`) when striped is enabled
- **Hover**: `hover:bg-white/10 transition-colors duration-200`
- **Padding**: Consistent `px-6 py-4` for cells

### Custom Styling

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function CustomStyledTable() {
  return (
    <GlassTable
      className="shadow-2xl border-2 border-blue-200"
      data={data}
      columns={columns}
      style={{
        borderRadius: '1rem',
        overflow: 'hidden'
      }}
    />
  );
}
```

## Accessibility

The GlassTable component includes accessibility features:

- **Semantic HTML**: Uses proper `<table>`, `<thead>`, `<tbody>` structure
- **Text Contrast**: Proper contrast ratios for readability
- **Hover States**: Clear visual feedback for interactive elements

### Accessibility Best Practices

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';

function AccessibleTable() {
  return (
    <div>
      <h2 id="table-title">User Data</h2>
      <GlassTable
        aria-labelledby="table-title"
        role="table"
        data={data}
        columns={columns}
      />
    </div>
  );
}
```

## Limitations & Extensions

The current GlassTable implementation is focused on display. For advanced features, consider:

### Manual Sorting Implementation

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { useState } from 'react';
import { ChevronUp, ChevronDown } from 'lucide-react';

function SortableTable() {
  const [data, setData] = useState([
    { name: "John", age: 30, city: "New York" },
    { name: "Jane", age: 25, city: "Los Angeles" },
    { name: "Bob", age: 35, city: "Chicago" },
  ]);
  
  const [sortConfig, setSortConfig] = useState<{
    key: string;
    direction: 'asc' | 'desc';
  } | null>(null);

  const handleSort = (key: string) => {
    let direction: 'asc' | 'desc' = 'asc';
    
    if (sortConfig && sortConfig.key === key && sortConfig.direction === 'asc') {
      direction = 'desc';
    }
    
    const sortedData = [...data].sort((a, b) => {
      if (a[key] < b[key]) return direction === 'asc' ? -1 : 1;
      if (a[key] > b[key]) return direction === 'asc' ? 1 : -1;
      return 0;
    });
    
    setData(sortedData);
    setSortConfig({ key, direction });
  };

  return (
    <GlassTable>
      <thead>
        <tr>
          {['name', 'age', 'city'].map((key) => (
            <th
              key={key}
              className="px-6 py-4 text-left text-gray-900 font-semibold cursor-pointer hover:bg-white/10 transition-colors"
              onClick={() => handleSort(key)}
            >
              <div className="flex items-center gap-2">
                {key.charAt(0).toUpperCase() + key.slice(1)}
                {sortConfig?.key === key && (
                  sortConfig.direction === 'asc' 
                    ? <ChevronUp size={16} />
                    : <ChevronDown size={16} />
                )}
              </div>
            </th>
          ))}
        </tr>
      </thead>
      <tbody>
        {data.map((row, index) => (
          <tr
            key={index}
            className={`${index % 2 === 1 ? 'bg-white/5' : ''} hover:bg-white/10 transition-colors duration-200`}
          >
            <td className="px-6 py-4 text-gray-700">{row.name}</td>
            <td className="px-6 py-4 text-gray-700">{row.age}</td>
            <td className="px-6 py-4 text-gray-700">{row.city}</td>
          </tr>
        ))}
      </tbody>
    </GlassTable>
  );
}
```

### Manual Pagination Implementation

```tsx
import { GlassTable } from '@tuliocunha23/liquidui';
import { useState } from 'react';
import { ChevronLeft, ChevronRight } from 'lucide-react';

function PaginatedTable() {
  const allData = Array.from({ length: 50 }, (_, i) => ({
    id: i + 1,
    name: `User ${i + 1}`,
    email: `user${i + 1}@example.com`,
  }));

  const [currentPage, setCurrentPage] = useState(1);
  const itemsPerPage = 5;
  
  const totalPages = Math.ceil(allData.length / itemsPerPage);
  const startIndex = (currentPage - 1) * itemsPerPage;
  const paginatedData = allData.slice(startIndex, startIndex + itemsPerPage);

  const columns = [
    { key: "id", label: "ID", width: "20%" },
    { key: "name", label: "Name", width: "40%" },
    { key: "email", label: "Email", width: "40%" },
  ];

  return (
    <div>
      <GlassTable data={paginatedData} columns={columns} />
      
      <div className="flex items-center justify-between mt-4 px-4">
        <div className="text-sm text-gray-600">
          Showing {startIndex + 1} to {Math.min(startIndex + itemsPerPage, allData.length)} of {allData.length} results
        </div>
        
        <div className="flex items-center gap-2">
          <button
            onClick={() => setCurrentPage(prev => Math.max(prev - 1, 1))}
            disabled={currentPage === 1}
            className="p-2 border border-gray-300 rounded disabled:opacity-50 disabled:cursor-not-allowed hover:bg-gray-50 transition-colors"
          >
            <ChevronLeft size={16} />
          </button>
          
          <span className="px-3 py-2 text-sm">
            {currentPage} of {totalPages}
          </span>
          
          <button
            onClick={() => setCurrentPage(prev => Math.min(prev + 1, totalPages))}
            disabled={currentPage === totalPages}
            className="p-2 border border-gray-300 rounded disabled:opacity-50 disabled:cursor-not-allowed hover:bg-gray-50 transition-colors"
          >
            <ChevronRight size={16} />
          </button>
        </div>
      </div>
    </div>
  );
}
```

## Related Components

- [`Dropdown`](/components/dropdown) - For row action menus
- [`Input`](/components/input) - For table search and filters  
- [`Button`](/components/button) - For table actions and pagination
- [`Loading`](/components/loading) - For table loading states
