# Header

This document serves to showcase the **Header** component.

import { ComponentShowcase } from "../../components/ComponentShowcase";

```jsx
import { GlassHeader } from "glass-ui-tulio";
```

<ComponentShowcase>
  ```jsx
  // Basic Header with Title and Subtitle
  <GlassHeader
    title="Dashboard"
    subtitle="Welcome back, John! Here's what's happening with your projects."
  />
  ```

  ```jsx
  // Header with Actions - Buttons and Dropdown
  <GlassHeader
    title="Project Management"
    subtitle="Manage your team's workflow and track progress"
    actions={
      <div className="flex items-center gap-3">
        <button className="p-2 rounded-lg glass-effect hover:glass-effect-hover transition-all">
          <Search className="w-5 h-5 text-secondary" />
        </button>
        <button className="p-2 rounded-lg glass-effect hover:glass-effect-hover transition-all">
          <Bell className="w-5 h-5 text-secondary" />
        </button>
        <button className="px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary-hover transition-colors">
          <Plus className="w-4 h-4 mr-2" />
          New Project
        </button>
      </div>
    }
  />
  ```

  ```jsx
  // Header with User Profile and Status
  <GlassHeader
    title={
      <div className="flex items-center gap-3">
        <div className="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600"></div>
        <span>Analytics Hub</span>
      </div>
    }
    subtitle="Real-time insights and performance metrics"
    actions={
      <div className="flex items-center gap-4">
        <div className="flex items-center gap-2 px-3 py-1 rounded-full glass-effect">
          <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
          <span className="text-sm text-secondary">Live</span>
        </div>
        <div className="flex items-center gap-3">
          <button className="p-2 rounded-lg glass-effect hover:glass-effect-hover transition-all">
            <Settings className="w-5 h-5 text-secondary" />
          </button>
          <div className="flex items-center gap-2 px-3 py-2 rounded-lg glass-effect">
            <div className="w-6 h-6 rounded-full bg-gradient-to-br from-emerald-400 to-cyan-500"></div>
            <span className="text-sm font-medium text-primary">Admin</span>
            <ChevronDown className="w-4 h-4 text-secondary" />
          </div>
        </div>
      </div>
    }
  />
  ```

  ```jsx
  // Minimal Header with Custom Styling
  <GlassHeader
    title="Clean & Simple"
    className="bg-white/5 border-white/10"
    actions={
      <div className="flex items-center gap-2">
        <button className="p-2 rounded-lg glass-effect hover:glass-effect-hover transition-all">
          <Download className="w-5 h-5 text-secondary" />
        </button>
        <button className="p-2 rounded-lg glass-effect hover:glass-effect-hover transition-all">
          <Share className="w-5 h-5 text-secondary" />
        </button>
      </div>
    }
  />
  ```
</ComponentShowcase>

## Props

| Name | Type | Default | Description |
|------|------|---------|-------------|
| title | `React.ReactNode` | — | Main header title content |
| subtitle | `React.ReactNode` | — | Optional subtitle displayed below the title |
| actions | `React.ReactNode` | — | Optional action elements (buttons, etc.) to display |
| className | `string` | — | Additional CSS classes to apply |

## Examples
