import { ComponentShowcase } from "../../components/ComponentShowcase";
import { HeroExamples } from "../../components/ExampleComponents";

# Hero

A stunning glassmorphism hero component with liquid effects and physics-based animations, perfect for creating captivating landing page headers with Apple-inspired aesthetics.

## Basic Usage

<ComponentShowcase
  title="Basic Hero"
  description="Simple hero section with title, subtitle, and glassmorphism effects"
  code={`import { GlassHero } from 'liquidify';

<GlassHero
        title="Welcome to LiqUIdify"
  subtitle="The future of UI design with glassmorphism components that flow like liquid glass."
/>`}
>
  <HeroExamples />
</ComponentShowcase>

## With Call-to-Action

<ComponentShowcase
  title="Hero with CTA Buttons"
  description="Hero section with call-to-action buttons and interactive elements"
  code={`<GlassHero
  title="Build Beautiful UIs"
  subtitle="Experience the magic of liquid glass components with physics-based interactions."
  actions={
    <div className="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="/getting-started">
        <GlassButton variant="primary" size="lg">
          Get Started
        </GlassButton>
      </a>
      <a href="/components">
        <GlassButton variant="secondary" size="lg">
          View Components
        </GlassButton>
      </a>
    </div>
  }
/>`}
>
  <HeroExamples />
</ComponentShowcase>

## With Background Elements

<ComponentShowcase
  title="Enhanced Hero with Background"
  description="Hero with animated background elements and gradient effects"
  code={`<GlassHero
  title={
    <span className="hero-text bg-gradient-to-r from-blue-600 to-green-600 bg-clip-text text-transparent">
      Liquid Glass Components
    </span>
  }
  subtitle="Build stunning interfaces with Apple-inspired glassmorphism design"
  variant="elevated"
  background="gradient"
  actions={
    <div className="flex items-center gap-6">
      <a href="/components">
        <GlassButton variant="primary" size="lg">
          <div className="flex items-center gap-2">
            <div className="w-5 h-5 ios-logo apple-gradient"></div>
            Explore Components
          </div>
        </GlassButton>
      </a>
      <a href="/demo" className="text-foreground hover:text-blue-600 transition-colors body-text">
        Watch Demo →
      </a>
    </div>
  }
/>`}
>
  <HeroExamples />
</ComponentShowcase>

## Full-Screen Hero

<ComponentShowcase
  title="Full-Screen Hero"
  description="Immersive full-screen hero with centered content and ambient effects"
  code={`<GlassHero
  title="The Future of UI"
  subtitle="Experience components that adapt, flow, and respond with lifelike physics."
  variant="fullscreen"
  size="xl"
  className="min-h-screen"
  actions={
    <div className="space-y-6">
      <div className="flex flex-col sm:flex-row gap-4 justify-center">
        <GlassButton variant="primary" size="lg" className="px-8">
          Start Building
        </GlassButton>
        <GlassButton variant="ghost" size="lg" className="px-8">
          Learn More
        </GlassButton>
      </div>
      <div className="flex items-center justify-center gap-8 text-sm text-muted-foreground body-text">
        <div className="flex items-center gap-2">
          <div className="w-4 h-4 ios-logo apple-gradient-2"></div>
          <span>30+ Components</span>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-4 h-4 ios-logo apple-gradient-3"></div>
          <span>TypeScript</span>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-4 h-4 ios-logo apple-gradient-4"></div>
          <span>Accessible</span>
        </div>
      </div>
    </div>
  }
/>`}
>
  <HeroExamples />
</ComponentShowcase>

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `title` | `React.ReactNode` | - | Main hero title content |
| `subtitle` | `React.ReactNode` | - | Hero subtitle or description |
| `actions` | `React.ReactNode` | - | Call-to-action buttons or elements |
| `background` | `"none" \| "gradient" \| "particles" \| "waves"` | `"none"` | Background animation type |
| `variant` | `"default" \| "minimal" \| "elevated" \| "fullscreen"` | `"default"` | Visual style variant |
| `size` | `"sm" \| "md" \| "lg" \| "xl"` | `"lg"` | Hero section size |
| `alignment` | `"left" \| "center" \| "right"` | `"center"` | Content alignment |
| `className` | `string` | - | Additional CSS classes |

### Physics Properties

The GlassHero includes advanced interactive features:

- **Parallax Effects**: Multi-layer background movement on scroll
- **Particle Animation**: Floating glass particles with realistic physics
- **Adaptive Blur**: Dynamic blur adjustment based on content
- **Magnetic Interactions**: Buttons that respond to cursor proximity
- **Liquid Morphing**: Smooth shape transitions and hover effects

### Background Variants

- **None**: Clean hero without background effects
- **Gradient**: Animated gradient background with color transitions
- **Particles**: Floating glass particles with physics simulation
- **Waves**: Liquid wave animation with glassmorphism effects

### Accessibility

- Semantic HTML structure with proper heading hierarchy
- Screen reader compatible with ARIA labels
- Keyboard navigation support for interactive elements
- High contrast mode support
- Motion preferences respect (reduced motion)

## Usage Guidelines

### Best Practices

- Use clear, compelling headlines that communicate value
- Keep call-to-action buttons prominent and actionable
- Ensure sufficient contrast for text readability
- Consider mobile responsiveness for all content
- Use background effects sparingly to avoid distraction

### When to Use

- **Landing Pages**: Main website entry point and first impression
- **Product Launch**: Introducing new features or products
- **Campaign Pages**: Marketing and promotional content
- **App Onboarding**: Welcome screens and getting started flows

## Examples

### Product Landing Hero

```tsx
import { GlassHero, GlassButton } from 'liquidify';
import { ArrowRight, Play } from 'lucide-react';

function ProductHero() {
  return (
    <GlassHero
      title={
        <div className="space-y-2">
          <h1 className="text-display-4xl font-display">
            Revolutionary Design
          </h1>
          <p className="text-subtitle-xl text-muted-foreground">
            The future of digital experiences
          </p>
          <p className="text-body-xl">
            Experience the perfect balance of bold headlines and readable content.
          </p>
        </div>
      }
      subtitle="Build beautiful, responsive interfaces with Apple-inspired glassmorphism design and physics-based interactions."
      variant="elevated"
      background="particles"
      actions={
        <div className="flex flex-col sm:flex-row gap-4 items-center">
          <a href="/getting-started">
            <GlassButton variant="primary" size="lg" className="px-8 py-4">
              <div className="flex items-center gap-2">
                Get Started
                <ArrowRight className="w-5 h-5" />
              </div>
            </GlassButton>
          </a>
          <a href="/demo">
            <GlassButton variant="ghost" size="lg" className="px-8 py-4">
              <div className="flex items-center gap-2">
                <Play className="w-5 h-5" />
                Watch Demo
              </div>
            </GlassButton>
          </a>
        </div>
      }
    />
  );
}
```

### SaaS Application Hero

```tsx
import { GlassHero, GlassButton, GlassCard } from 'liquidify';
import { Zap, Shield, Sparkles } from 'lucide-react';

function SaaSHero() {
  const features = [
    { icon: Zap, text: "Lightning Fast" },
    { icon: Shield, text: "Enterprise Secure" },
    { icon: Sparkles, text: "AI Powered" }
  ];

  return (
    <GlassHero
      title="Transform Your Workflow"
      subtitle="The only tool you need to manage projects, collaborate with teams, and deliver results faster than ever."
      variant="fullscreen"
      background="gradient"
      actions={
        <div className="space-y-8">
          <div className="flex flex-col sm:flex-row gap-4">
            <a href="/signup">
              <GlassButton variant="primary" size="lg">
                Start Free Trial
              </GlassButton>
            </a>
            <a href="/demo">
              <GlassButton variant="secondary" size="lg">
                Schedule Demo
              </GlassButton>
            </a>
          </div>
          
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-4 max-w-md mx-auto">
            {features.map((feature, index) => (
              <GlassCard key={index} className="p-4 text-center">
                <feature.icon className="w-6 h-6 mx-auto mb-2 text-blue-600" />
                <span className="text-sm text-foreground body-text">{feature.text}</span>
              </GlassCard>
            ))}
          </div>
        </div>
      }
    />
  );
}
```

### Minimal Hero

```tsx
import { GlassHero, GlassButton } from 'liquidify';

function MinimalHero() {
  return (
    <GlassHero
      title="Simple. Powerful. Beautiful."
      subtitle="Everything you need, nothing you don't."
      variant="minimal"
      size="md"
      alignment="left"
      className="py-20"
      actions={
        <a href="/getting-started">
          <GlassButton variant="primary">
            Get Started
          </GlassButton>
        </a>
      }
    />
  );
}
```

### Animated Hero with Stats

```tsx
import { GlassHero, GlassButton } from 'liquidify';
import { useState, useEffect } from 'react';

function AnimatedHero() {
  const [stats, setStats] = useState({ users: 0, components: 0, downloads: 0 });

  useEffect(() => {
    // Animate numbers on mount
    const timer = setInterval(() => {
      setStats(prev => ({
        users: Math.min(prev.users + 127, 10000),
        components: Math.min(prev.components + 1, 30),
        downloads: Math.min(prev.downloads + 289, 0)
      }));
    }, 50);

    return () => clearInterval(timer);
  }, []);

  return (
    <GlassHero
      title="Trusted by Developers Worldwide"
              subtitle="Join thousands of developers building beautiful UIs with LiqUIdify"
      background="waves"
      actions={
        <div className="space-y-8">
          <a href="/community">
            <GlassButton variant="primary" size="lg">
              Join the Community
            </GlassButton>
          </a>
          
          <div className="grid grid-cols-3 gap-8 text-center">
            <div>
              <div className="text-3xl font-bold text-blue-600 hero-text">
                {stats.users.toLocaleString()}+
              </div>
              <div className="text-sm text-muted-foreground body-text">Active Users</div>
            </div>
            <div>
              <div className="text-3xl font-bold text-green-600 hero-text">
                {stats.components}+
              </div>
              <div className="text-sm text-muted-foreground body-text">Components</div>
            </div>
            <div>
              <div className="text-3xl font-bold text-purple-600 hero-text">
                Soon
              </div>
              <div className="text-sm text-muted-foreground body-text">Launch Day</div>
            </div>
          </div>
        </div>
      }
    />
  );
}
```
