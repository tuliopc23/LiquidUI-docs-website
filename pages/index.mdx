import { GlassCard, GlassButton, GlassBadge } from "../components/GlassComponents";
import { ComponentShowcase } from "../components/ComponentShowcase";
import { motion } from "framer-motion";
import { Sparkles, Zap, Palette, Shield, Code, Rocket } from "lucide-react";

<div className="w-full min-h-screen relative overflow-hidden">
  {/* Hero Section */}
  <motion.section 
    className="relative z-10 flex flex-col items-center justify-center text-center py-20 px-4"
    initial={{ opacity: 0, y: 30 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.8 }}
    role="banner"
    aria-labelledby="hero-title"
  >
    <motion.div
      initial={{ scale: 0.8, opacity: 0 }}
      animate={{ scale: 1, opacity: 1 }}
      transition={{ delay: 0.2, duration: 0.6 }}
      className="mb-6"
    >
      <GlassBadge variant="outline" className="mb-4">
        <Sparkles className="w-4 h-4 mr-2" />
        Physics-Based Glassmorphism
      </GlassBadge>
    </motion.div>

    <motion.h1 
      className="text-5xl md:text-7xl font-bold tracking-tight mb-6"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.4, duration: 0.6 }}
    >
      <span className="bg-gradient-to-r from-blue-400 via-purple-500 to-cyan-400 bg-clip-text text-transparent">
        LiquidUI
      </span>
      <br />
      <span className="text-foreground">for React</span>
    </motion.h1>
    
    <motion.p 
      className="text-xl md:text-2xl text-muted-foreground max-w-3xl mb-8 leading-relaxed"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.6, duration: 0.6 }}
    >
      A revolutionary component library featuring{" "}
      <span className="text-primary font-semibold">physics-based interactions</span>,{" "}
      <span className="text-purple-400 font-semibold">content-aware adaptation</span>, and{" "}
      <span className="text-cyan-400 font-semibold">magnetic hover effects</span>{" "}
      with stunning glassmorphism design.
    </motion.p>

    <motion.div 
      className="flex flex-col sm:flex-row gap-4 mb-12"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.8, duration: 0.6 }}
    >
      <GlassButton size="lg" className="text-lg px-8 py-4">
        <a href="/getting-started" className="flex items-center gap-2">
          <Rocket className="w-5 h-5" />
          Get Started
        </a>
      </GlassButton>
      <GlassButton variant="secondary" size="lg" className="text-lg px-8 py-4">
        <a href="/components" className="flex items-center gap-2">
          <Code className="w-5 h-5" />
          Browse Components
        </a>
      </GlassButton>
    </motion.div>

    {/* Stats */}
    <motion.div 
      className="flex flex-wrap justify-center gap-8 text-center"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 1.0, duration: 0.6 }}
    >
      <div className="glass-effect px-4 py-2 rounded-lg">
        <div className="text-2xl font-bold text-primary">30+</div>
        <div className="text-sm text-muted-foreground">Components</div>
      </div>
      <div className="glass-effect px-4 py-2 rounded-lg">
        <div className="text-2xl font-bold text-purple-400">100%</div>
        <div className="text-sm text-muted-foreground">TypeScript</div>
      </div>
      <div className="glass-effect px-4 py-2 rounded-lg">
        <div className="text-2xl font-bold text-cyan-400">A11Y</div>
        <div className="text-sm text-muted-foreground">Accessible</div>
      </div>
    </motion.div>
  </motion.section>

  {/* Features Grid */}
  <motion.div 
    className="relative z-10 max-w-7xl mx-auto px-4 py-16"
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ delay: 1.2, duration: 0.8 }}
  >
    <motion.h2 
      className="text-4xl font-bold text-center mb-4"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 1.4, duration: 0.6 }}
    >
      Why Choose LiquidUI?
    </motion.h2>
    <motion.p 
      className="text-xl text-center text-muted-foreground mb-12"
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 1.6, duration: 0.6 }}
    >
      Experience the future of React component libraries
    </motion.p>

    <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {[
        {
          icon: <Zap className="w-8 h-8 text-yellow-400" />,
          title: "Physics-Based Interactions",
          description: "Real-time spring animations and magnetic hover effects that respond to user interaction with natural, fluid motion.",
          delay: 1.8
        },
        {
          icon: <Palette className="w-8 h-8 text-purple-400" />,
          title: "Content-Aware Adaptation",
          description: "Smart components that automatically adjust their appearance and behavior based on content and context.",
          delay: 2.0
        },
        {
          icon: <Shield className="w-8 h-8 text-green-400" />,
          title: "Accessibility First",
          description: "Built with WCAG 2.1 standards, screen reader support, and keyboard navigation out of the box.",
          delay: 2.2
        },
        {
          icon: <Code className="w-8 h-8 text-blue-400" />,
          title: "TypeScript Native",
          description: "Fully typed components with IntelliSense support, auto-completion, and compile-time safety.",
          delay: 2.4
        },
        {
          icon: <Sparkles className="w-8 h-8 text-cyan-400" />,
          title: "Glassmorphism Design",
          description: "Stunning backdrop blur effects, transparency layers, and ethereal visual aesthetics.",
          delay: 2.6
        },
        {
          icon: <Rocket className="w-8 h-8 text-orange-400" />,
          title: "Performance Optimized",
          description: "Tree-shakeable components, minimal bundle size, and optimized for production applications.",
          delay: 2.8
        }
      ].map((feature, index) => (
        <motion.div
          key={index}
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: feature.delay, duration: 0.6 }}
        >
          <GlassCard className="h-full p-8 hover:scale-105 transition-transform duration-300">
            <div className="flex flex-col items-center text-center h-full">
              <motion.div
                className="mb-4 p-3 glass-effect rounded-lg"
                whileHover={{ scale: 1.1, rotate: 5 }}
                transition={{ type: "spring", stiffness: 400, damping: 10 }}
              >
                {feature.icon}
              </motion.div>
              <h3 className="text-xl font-semibold mb-3 text-foreground">
                {feature.title}
              </h3>
              <p className="text-muted-foreground leading-relaxed flex-grow">
                {feature.description}
              </p>
            </div>
          </GlassCard>
        </motion.div>
      ))}
    </div>
  </motion.div>

  {/* Call to Action */}
  <motion.div 
    className="relative z-10 text-center py-20 px-4"
    initial={{ opacity: 0, y: 30 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ delay: 3.0, duration: 0.8 }}
  >
    <div className="glass-card max-w-4xl mx-auto p-12">
      <h2 className="text-4xl font-bold mb-6">
        Ready to Build Something Amazing?
      </h2>
      <p className="text-xl text-muted-foreground mb-8">
        Join thousands of developers creating beautiful, accessible, and performant UIs with LiquidUI.
      </p>
      <div className="flex flex-col sm:flex-row gap-4 justify-center">
        <GlassButton size="lg" className="text-lg px-8 py-4">
          <a href="/getting-started">Start Building</a>
        </GlassButton>
        <GlassButton variant="outline" size="lg" className="text-lg px-8 py-4">
          <a href="https://github.com/tuliopc23/glass-ui" target="_blank" rel="noopener noreferrer">
            View on GitHub
          </a>
        </GlassButton>
      </div>
    </div>
  </motion.div>

  {/* Background Effects */}
  <div className="absolute inset-0 pointer-events-none">
    <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl" />
    <div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl" />
    <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-cyan-500/5 rounded-full blur-3xl" />
  </div>
</div>