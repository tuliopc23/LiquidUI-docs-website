---
title: Theming
---

import { Callout, Tabs, Tab } from 'nextra/components'
import { GlassCard, GlassButton, GlassBadge } from "glass-ui-tulio";
import { ComponentShowcase } from "../components/ComponentShowcase";
import { motion } from "framer-motion";
import { Palette, Sparkles, Settings, Code, Sun, Moon } from "lucide-react";

# Theming

Customize LiquidiUI to match your brand and design requirements with our powerful theming system.

<div className="glass-card p-6 my-8">
  <div className="flex items-center gap-3 mb-4">
    <Palette className="w-6 h-6 text-primary" />
    <h2 className="text-xl font-semibold m-0">Design System Foundation</h2>
  </div>
  <p className="text-muted-foreground mb-4">
    LiquidiUI's theming system is built on CSS custom properties and provides deep customization capabilities while maintaining the signature glassmorphism aesthetic.
  </p>
  <div className="flex flex-wrap gap-2">
    <GlassBadge variant="outline">CSS Variables</GlassBadge>
    <GlassBadge variant="outline">Dark/Light Mode</GlassBadge>
    <GlassBadge variant="outline">Custom Properties</GlassBadge>
    <GlassBadge variant="outline">Brand Colors</GlassBadge>
  </div>
</div>

## Theme Toggle

<ComponentShowcase
  title="Theme Switching"
  description="Built-in theme toggle component"
  code={`// Theme toggle will be available in the full package
// For now, you can implement a custom toggle using CSS variables

function ThemeToggle() {
  const [theme, setTheme] = useState('dark')
  
  const toggleTheme = () => {
    const newTheme = theme === 'dark' ? 'light' : 'dark'
    setTheme(newTheme)
    document.documentElement.setAttribute('data-theme', newTheme)
  }
  
  return (
    <button onClick={toggleTheme} className="glass-effect p-2 rounded-lg">
      {theme === 'dark' ? '🌙' : '☀️'}
    </button>
  )
}`}
>
  <div className="flex items-center gap-4">
    <button className="glass-effect p-2 rounded-lg hover:bg-white/10 transition-colors">
      🌙
    </button>
    <span className="text-sm text-muted-foreground">
      Theme toggle (implementation shown in code)
    </span>
  </div>
</ComponentShowcase>

## Color System

### Primary Colors

<ComponentShowcase
  title="Color Palette"
  description="Core color system with glassmorphism support"
>
  <div className="grid gap-4 md:grid-cols-3 w-full">
    <div className="glass-effect p-4 rounded-lg">
      <div className="w-full h-12 bg-primary rounded mb-3"></div>
      <div className="text-sm font-medium">Primary</div>
      <div className="text-xs text-muted-foreground">hsl(217, 91%, 60%)</div>
    </div>
    <div className="glass-effect p-4 rounded-lg">
      <div className="w-full h-12 bg-secondary rounded mb-3"></div>
      <div className="text-sm font-medium">Secondary</div>
      <div className="text-xs text-muted-foreground">hsl(217, 33%, 18%)</div>
    </div>
    <div className="glass-effect p-4 rounded-lg">
      <div className="w-full h-12 bg-accent rounded mb-3"></div>
      <div className="text-sm font-medium">Accent</div>
      <div className="text-xs text-muted-foreground">hsl(217, 33%, 18%)</div>
    </div>
  </div>
</ComponentShowcase>

### Semantic Colors

<ComponentShowcase
  title="Semantic Color System"
  description="Colors with specific meanings and use cases"
>
  <div className="grid gap-3 md:grid-cols-2 lg:grid-cols-4 w-full">
    <GlassCard className="p-3 text-center">
      <div className="w-full h-8 bg-green-500 rounded mb-2"></div>
      <div className="text-xs font-medium">Success</div>
    </GlassCard>
    <GlassCard className="p-3 text-center">
      <div className="w-full h-8 bg-yellow-500 rounded mb-2"></div>
      <div className="text-xs font-medium">Warning</div>
    </GlassCard>
    <GlassCard className="p-3 text-center">
      <div className="w-full h-8 bg-red-500 rounded mb-2"></div>
      <div className="text-xs font-medium">Error</div>
    </GlassCard>
    <GlassCard className="p-3 text-center">
      <div className="w-full h-8 bg-blue-500 rounded mb-2"></div>
      <div className="text-xs font-medium">Info</div>
    </GlassCard>
  </div>
</ComponentShowcase>

## CSS Variables

### Core Variables

LiquidiUI uses CSS custom properties for easy theming:

```css
:root {
  /* Colors */
  --background: 222.2 84% 4.9%;
  --foreground: 210 40% 98%;
  --primary: 217.2 91.2% 59.8%;
  --primary-foreground: 222.2 84% 4.9%;
  --secondary: 217.2 32.6% 17.5%;
  --secondary-foreground: 210 40% 98%;
  --muted: 217.2 32.6% 17.5%;
  --muted-foreground: 215 20.2% 65.1%;
  --accent: 217.2 32.6% 17.5%;
  --accent-foreground: 210 40% 98%;
  --destructive: 0 62.8% 30.6%;
  --destructive-foreground: 210 40% 98%;
  --border: 217.2 32.6% 17.5%;
  --input: 217.2 32.6% 17.5%;
  --ring: 224.3 76.3% 94.1%;
  
  /* Sizing */
  --radius: 0.75rem;
}
```

### Light Theme

```css
[data-theme="light"] {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --primary: 217.2 91.2% 59.8%;
  --primary-foreground: 0 0% 98%;
  --secondary: 210 40% 96%;
  --secondary-foreground: 222.2 84% 4.9%;
  --muted: 210 40% 96%;
  --muted-foreground: 215.4 16.3% 46.9%;
  --accent: 210 40% 96%;
  --accent-foreground: 222.2 84% 4.9%;
  --border: 214.3 31.8% 91.4%;
  --input: 214.3 31.8% 91.4%;
  --ring: 221.2 83.2% 53.3%;
}
```

## Custom Brand Colors

### Step 1: Define Your Colors

Create your brand color palette:

```css
:root {
  /* Your brand colors */
  --brand-blue: 210 100% 50%;
  --brand-purple: 270 100% 60%;
  --brand-green: 120 100% 40%;
  
  /* Apply to LiquidiUI variables */
  --primary: var(--brand-blue);
  --accent: var(--brand-purple);
}
```

### Step 2: Create Theme Variants

<ComponentShowcase
  title="Brand Color Examples"
  description="Different brand color applications"
>
  <div className="space-y-4 w-full">
    <div className="flex gap-3">
      <GlassButton style={{ '--primary': '210 100% 50%' }}>
        Tech Blue
      </GlassButton>
      <GlassButton style={{ '--primary': '270 100% 60%' }}>
        Creative Purple  
      </GlassButton>
      <GlassButton style={{ '--primary': '120 100% 40%' }}>
        Nature Green
      </GlassButton>
    </div>
  </div>
</ComponentShowcase>

## Advanced Customization

### Glass Effect Properties

Customize the glassmorphism effects:

```css
.custom-glass {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.12);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
}
```

### Component-Specific Theming

Override specific component styles:

```css
/* Custom button theme */
.my-button {
  --glass-primary: #ff6b6b;
  --glass-primary-foreground: #ffffff;
  --glass-blur: 15px;
  --glass-opacity: 0.1;
}

/* Custom card theme */
.my-card {
  --glass-background: rgba(255, 255, 255, 0.05);
  --glass-border: rgba(255, 255, 255, 0.15);
  --glass-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
}
```

## Theme Presets

### Corporate Theme

<ComponentShowcase
  title="Corporate Theme"
  description="Professional, business-focused theme"
  code={`// Corporate theme variables
const corporateTheme = {
  '--primary': '210 100% 45%',
  '--secondary': '210 15% 20%',
  '--accent': '210 100% 55%',
  '--background': '210 15% 8%',
  '--foreground': '210 15% 95%'
}`}
>
  <div 
    className="glass-card p-6 max-w-md"
    style={{
      '--primary': '210 100% 45%',
      '--secondary': '210 15% 20%',
      '--accent': '210 100% 55%'
    }}
  >
    <h3 className="text-lg font-semibold mb-3">Corporate Dashboard</h3>
    <p className="text-muted-foreground mb-4">
      Professional and reliable interface design.
    </p>
    <GlassButton>View Reports</GlassButton>
  </div>
</ComponentShowcase>

### Creative Theme

<ComponentShowcase
  title="Creative Theme"
  description="Vibrant, artistic theme for creative projects"
  code={`// Creative theme variables
const creativeTheme = {
  '--primary': '270 100% 60%',
  '--secondary': '320 100% 50%',
  '--accent': '45 100% 55%',
  '--background': '270 15% 5%',
  '--foreground': '270 5% 95%'
}`}
>
  <div 
    className="glass-card p-6 max-w-md"
    style={{
      '--primary': '270 100% 60%',
      '--secondary': '320 100% 50%',
      '--accent': '45 100% 55%'
    }}
  >
    <h3 className="text-lg font-semibold mb-3">Creative Portfolio</h3>
    <p className="text-muted-foreground mb-4">
      Express your creativity with vibrant colors.
    </p>
    <GlassButton>View Gallery</GlassButton>
  </div>
</ComponentShowcase>

## Typography Customization

### Font Families

```css
:root {
  --font-sans: 'Inter', system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
  --font-heading: 'Cal Sans', 'Inter', sans-serif;
}

/* Apply custom fonts */
.prose h1, .prose h2, .prose h3 {
  font-family: var(--font-heading);
}
```

### Font Sizes

```css
:root {
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;
  --text-4xl: 2.25rem;
}
```

## Dark Mode Implementation

### Automatic Detection

```tsx
import { ThemeProvider } from 'glass-ui-tulio'

function App() {
  return (
    <ThemeProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
    >
      {/* Your app */}
    </ThemeProvider>
  )
}
```

### Manual Control

```tsx
import { useTheme } from 'glass-ui-tulio'

function ThemeControls() {
  const { theme, setTheme } = useTheme()
  
  return (
    <div className="flex gap-2">
      <GlassButton 
        variant={theme === 'light' ? 'default' : 'outline'}
        onClick={() => setTheme('light')}
      >
        <Sun className="w-4 h-4" />
        Light
      </GlassButton>
      <GlassButton 
        variant={theme === 'dark' ? 'default' : 'outline'}
        onClick={() => setTheme('dark')}
      >
        <Moon className="w-4 h-4" />
        Dark
      </GlassButton>
    </div>
  )
}
```

## Responsive Theming

### Breakpoint-Based Themes

```css
/* Mobile theme adjustments */
@media (max-width: 768px) {
  :root {
    --glass-blur: 8px;
    --glass-opacity: 0.12;
    --radius: 0.5rem;
  }
}

/* Desktop theme enhancements */
@media (min-width: 1024px) {
  :root {
    --glass-blur: 15px;
    --glass-opacity: 0.08;
    --radius: 1rem;
  }
}
```

## Performance Considerations

### CSS Variable Optimization

- Use HSL color space for better interpolation
- Minimize the number of custom properties
- Group related variables together
- Use fallback values for better browser support

### Best Practices

1. **Consistent Naming**: Use semantic names for variables
2. **Fallback Values**: Always provide fallbacks for custom properties
3. **Performance**: Avoid excessive nesting of glass effects
4. **Accessibility**: Ensure sufficient contrast ratios
5. **Testing**: Test themes across different devices and browsers

## Examples

### Complete Theme Implementation

```tsx
// theme.ts
export const themes = {
  corporate: {
    '--primary': '210 100% 45%',
    '--secondary': '210 15% 20%',
    '--accent': '210 100% 55%',
    '--background': '210 15% 8%',
    '--foreground': '210 15% 95%',
    '--muted': '210 15% 15%',
    '--muted-foreground': '210 15% 70%',
    '--border': '210 15% 18%',
    '--radius': '0.5rem'
  },
  creative: {
    '--primary': '270 100% 60%',
    '--secondary': '320 100% 50%',
    '--accent': '45 100% 55%',
    '--background': '270 15% 5%',
    '--foreground': '270 5% 95%',
    '--muted': '270 15% 10%',
    '--muted-foreground': '270 5% 70%',
    '--border': '270 15% 15%',
    '--radius': '1rem'
  }
}

// Apply theme
function applyTheme(themeName: keyof typeof themes) {
  const theme = themes[themeName]
  const root = document.documentElement
  
  Object.entries(theme).forEach(([property, value]) => {
    root.style.setProperty(property, value)
  })
}
```

### Theme Selector Component

```tsx
import { useState } from 'react'
import { GlassButton, GlassCard } from 'glass-ui-tulio'

function ThemeSelector() {
  const [activeTheme, setActiveTheme] = useState('default')
  
  const themes = [
    { name: 'default', label: 'Default', primary: '217 91% 60%' },
    { name: 'corporate', label: 'Corporate', primary: '210 100% 45%' },
    { name: 'creative', label: 'Creative', primary: '270 100% 60%' },
    { name: 'nature', label: 'Nature', primary: '120 100% 40%' }
  ]
  
  return (
    <GlassCard className="p-6">
      <h3 className="text-lg font-semibold mb-4">Choose Theme</h3>
      <div className="grid gap-3 md:grid-cols-2">
        {themes.map((theme) => (
          <GlassButton
            key={theme.name}
            variant={activeTheme === theme.name ? 'default' : 'outline'}
            onClick={() => {
              setActiveTheme(theme.name)
              applyTheme(theme.name)
            }}
            className="flex items-center gap-2"
          >
            <div 
              className="w-4 h-4 rounded-full"
              style={{ background: `hsl(${theme.primary})` }}
            />
            {theme.label}
          </GlassButton>
        ))}
      </div>
    </GlassCard>
  )
}
```
